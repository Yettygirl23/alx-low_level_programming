#include <stdio.h>
#include <stdlib.h>

/**
 * main - lowercase
 *
 * Description: prints out lowercase alphabets
 *
 * Return: Always 0 (Sucess)
 */

int main()
{
	char letter;
	for (letter = 'a'; letter <= 'z'; lettter++)
	{putchar(letter); }

	putchar('\n');

	return (0);

	}
